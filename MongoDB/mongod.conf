# mongod.conf

# Store data in the /data/db directory
dbpath=/data/db

# Enable journaling for durability
journal=true

# Bind MongoDB to all network interfaces
bind_ip_all=true

# Allow remote connections to the database
# Warning: This allows external access to your MongoDB, consider using appropriate security measures
